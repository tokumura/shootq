<div class="col-xs-12">
  <h3>お知らせ</h3>
  <div class="alert alert-warning">
    <p>6月12日(木) 23:59 までにグループリーグ予想をしてください。</p>
  </div>

  <br/>
  <h3>ニュースピックアップ</h3><br/>
  <div class="row">
  <% @news_feeds.each_with_index do |nf, i| %>
    <div class="col-sm-4 col-md-3 ms-item">
      <div class="thumbnail">
        <a href="<%=nf.news_url%>" target="_blank">
          <img src="<%=nf.news_img_url%>" style="width:280px">
        </a>
        <div class="caption">
          <p><%=nf.title%></p>
        </div>
      </div>
    </div>
    <% if (i+1)%3 == 0 %>
      <br/>
    <% end %>
  <% end %>
  </div>
  <% if NewsFeed.all.size > 10 %>
    <a href="<%=show_pickups_news_feeds_path%>">もっと見る</a>
  <% end %>

  <br/>

  <h3>皆さんの意気込み</h3>
  <table class="table table-bordered">
    <thead>
      <th style="width:100px;">名前</th>
      <th style="">意気込み</th>
    </thead>
    <tbody>
      <% @users.each do |u| %>
      <tr>
        <td><%= u.user_info.name %></td>
        <td><%= u.user_info.voice %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
