<%= form_for(@news_feed) do |f| %>
  <% if @news_feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_feed.errors.count, "error") %> prohibited this news_feed from being saved:</h2>

      <ul>
      <% @news_feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :news_url %><br>
    <%= f.text_field :news_url %>
  </div>
  <div class="field">
    <%= f.label :news_img_url %><br>
    <%= f.text_field :news_img_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
